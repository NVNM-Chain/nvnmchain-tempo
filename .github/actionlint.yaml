# Labels actionlint does not know about. All runners used by this repository
# are GitHub-hosted; `ubuntu-24.04-arm` is listed here because older actionlint
# releases do not ship it in their built-in runner list.
self-hosted-runner:
  labels:
    - ubuntu-24.04-arm
